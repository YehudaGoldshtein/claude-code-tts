# machine-local config — copy .env.example to .env and edit
.env

# python virtualenv — recreated by install.ps1
.venv/
__pycache__/

# Kokoro model files (~350 MB) — downloaded by install.ps1
*.onnx
*.bin

# runtime state written by voice.ps1 / the hook
voice.off
voice.level
